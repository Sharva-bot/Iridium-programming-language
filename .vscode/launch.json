{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Current S++ File",
      "type": "node-terminal",
      "request": "launch",
      "command": "\"${workspaceFolder}\\s_+_+.exe\" \"${file}\"",
      "cwd": "${workspaceFolder}"
    }
  ]
}
